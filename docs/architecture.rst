Architecture
============

WikiMason keeps deterministic wiki logic in Python and exposes a
filesystem-backed CLI with configurable tool profiles. Runtime bridge
commands are not part of the product surface.
